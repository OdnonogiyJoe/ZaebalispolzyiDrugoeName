<Page x:Class="SavaDejurit.VisitList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SavaDejurit"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VisitList">

        <DockPanel Margin="5">
            <TextBlock Text="{Binding Title}" FontSize="16" HorizontalAlignment="Left" DockPanel.Dock="Top"/>
            <Button Content="Вернуться назад" DockPanel.Dock="Bottom" Width="100" Command="{Binding Back}" Padding="3" HorizontalAlignment="Center"/>
            <ListBox DockPanel.Dock="Top" ItemsSource="{Binding Visits}"/>
        </DockPanel>
</Page>
