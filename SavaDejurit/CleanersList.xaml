<Page x:Class="SavaDejurit.CleanersList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SavaDejurit"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CleanersList">

        <DockPanel Margin="5">
            <Button Content="Назад" DockPanel.Dock="Bottom" Width="200" Command="{Binding Return}" Padding="5" Margin="0 5 0 0"/>
            <Button Content="Выборка" DockPanel.Dock="Bottom" Width="200" Command="{Binding Random}" Padding="5" Margin="0 5 0 0"/>
            <ListBox DockPanel.Dock="Top"/>
        </DockPanel>
</Page>
